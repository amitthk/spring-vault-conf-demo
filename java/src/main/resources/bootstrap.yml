spring:
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: kv
      application-name: amitthk/vault-demo/dev
